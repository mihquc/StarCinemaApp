import React from 'react';
import { View, Image, TouchableOpacity } from 'react-native';

const Seat = ({item, style}) => {
  return(
    <TouchableOpacity style={style}>
      
    </TouchableOpacity>
  )
}

export default Seat;